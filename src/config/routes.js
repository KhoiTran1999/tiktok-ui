const routes = {
    home: '/',
    following: '/following',
    profile: '/profile/:userName',
    comment: '/profile/:userName/:videoId',
    live: '/live',
    messages: '/messages',
    upload: '/upload',
};

export default routes;
