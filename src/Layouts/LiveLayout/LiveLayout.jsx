import React from 'react';
import Header from '../../components/Header/Header';

const LiveLayout = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default LiveLayout;
